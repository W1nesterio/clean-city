Stage 16: mobile main screen and route task selection.

Changes:
- Home screen redesigned into a cleaner dashboard.
- Worker route screen now has explicit task selection cards.
- Selected tasks are optimized and shown as an ordered stop list.
- Route line still tries to use OSRM road routing and falls back to straight segments if the road service is unavailable.

Install: unpack this archive directly into the CleanCity2 project folder and rebuild the Android app.
